{
  "name": "@foxhound/api-client",
  "version": "0.1.0",
  "description": "Typed HTTP client for the Foxhound API — shared by CLI and MCP server",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@foxhound/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^3.2.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.4"
  }
}
