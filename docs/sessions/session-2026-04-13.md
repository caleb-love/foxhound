# Session Log — 2026-04-13

## Topic
Narrative-driven demo/test data planning and implementation for `apps/web`, with a forward path to a future marketing-site interactive demo.

## What was completed

### 1. Durable planning artifacts created

Created:
- `docs/plans/active/2026-04-13-marketing-demo-seed-data-plan.md`
- `docs/plans/active/2026-04-13-marketing-demo-scenario-catalog.md`
- `docs/plans/active/2026-04-13-shared-demo-domain-module-plan.md`

These documents define:
- the hero narrative (`Support Copilot`)
- scenario catalog and stable fixture naming
- seeded orgs / prompts / traces / datasets / evaluators / experiments / governance records
- the recommendation to use a shared package rather than `apps/web`-local fixtures

---

### 2. Shared demo package scaffolded

Created package:
- `packages/demo-domain`

Created files:
- `packages/demo-domain/package.json`
- `packages/demo-domain/tsconfig.json`
- `packages/demo-domain/src/index.ts`
- `packages/demo-domain/src/types.ts`
- `packages/demo-domain/src/support-copilot.ts`

This package is now the shared source of truth for demo-story data.

---

### 3. First vertical slice implemented in shared package

The package currently exports shared demo data and builders for the `Support Copilot` story.

Implemented exports include:
- orgs
- prompts and prompt versions
- scenarios
- curated traces
- background traces
- diff pairs
- regressions
- datasets
- evaluators
- experiments
- budget fixtures
- SLA fixtures
- notification fixtures
- replay target IDs
- `buildMarketingHeroDemo()`
- `buildLocalReviewDemo()`

---

### 4. Shared scenario set expanded beyond the initial refund story

The shared package now includes:

#### Prompt families
- `support-reply` v17 / v18 / v19
- `refund-policy-check` v3 / v4 / v5
- `escalation-triage` v7 / v8

#### Curated traces
- `trace_support_refund_v17_baseline`
- `trace_support_refund_v18_regression`
- `trace_support_refund_v19_fix`
- `trace_damage_receipt_v18_hallucination`
- `trace_vip_chargeback_v18_missed_escalation`
- `trace_vip_chargeback_v19_restored_escalation`
- `trace_kb_timeout_failed`
- `trace_kb_timeout_recovered`

#### Background traces
Added lightweight healthy traces to make the demo trace list feel more realistic.

#### Diff pairs
Includes refund regression/fix, hallucination regression, escalation recovery, and KB timeout recovery.

#### Datasets
- `refund-edge-cases`
- `escalation-policy-cases`
- `support-hallucination-guardrails`
- `latency-sensitive-common-questions`

#### Evaluators
- `refund_policy_correctness`
- `escalation_correctness`
- `policy_groundedness`
- `helpfulness`

#### Experiments
- `refund-recovery-v19`
- `escalation-threshold-tuning`
- `cheap-common-support-routing`

#### Governance data
Budgets, SLAs, and notifications now include multiple records rather than a single minimal record.

---

### 5. `apps/web` demo mode now consumes the shared package

Updated `apps/web` to use `@foxhound/demo-domain`.

Important files changed:
- `apps/web/package.json`
- `apps/web/tsconfig.json`
- `apps/web/app/api/demo/traces/route.ts`
- `apps/web/app/api/demo/traces/[id]/route.ts`

Notes:
- demo trace API now returns shared package data
- demo traces now include curated + background traces
- a temporary TS path alias was added in `apps/web/tsconfig.json` so the new package resolves immediately in the current workspace state

---

### 6. Demo pages wired to the shared narrative

Now powered by `buildLocalReviewDemo()`:

#### Overview / executive
- `apps/web/app/demo/page.tsx`
- `apps/web/app/demo/executive/page.tsx`

#### Investigate
- `apps/web/app/demo/traces/page.tsx`
- `apps/web/app/demo/traces/[id]/page.tsx`
- `apps/web/app/demo/diff/page.tsx`

#### Improve
- `apps/web/app/demo/prompts/page.tsx`
- `apps/web/app/demo/prompts/[id]/page.tsx`
- `apps/web/app/demo/prompts/[id]/diff/page.tsx`
- `apps/web/app/demo/datasets/page.tsx`
- `apps/web/app/demo/experiments/page.tsx`

#### Govern
- `apps/web/app/demo/regressions/page.tsx`
- `apps/web/app/demo/budgets/page.tsx`
- `apps/web/app/demo/slas/page.tsx`
- `apps/web/app/demo/notifications/page.tsx`

#### Layout copy
- `apps/web/app/demo/layout.tsx`

---

### 7. Demo cross-linking was polished

Updated components so demo-mode links stay inside `/demo` instead of jumping into non-demo routes.

Updated:
- `apps/web/components/traces/trace-detail-view.tsx`
- `apps/web/components/replay/replay-detail-view.tsx`
- `apps/web/components/diff/run-diff-view.tsx`
- `apps/web/app/demo/traces/[id]/page.tsx`

This made the shared demo UX much more coherent.

---

## Verification completed

Successfully ran and passed:
- `pnpm --filter @foxhound/demo-domain typecheck`
- `pnpm --filter @foxhound/demo-domain build`
- `pnpm --filter web typecheck`

These checks passed after the shared package scaffold and all demo-page wiring changes.

---

## Current architecture / durable truth

### Canonical demo-story source
- `packages/demo-domain`

### Main consumer currently wired
- `apps/web/app/demo/*`

### Narrative model
The primary hero story is:
- prompt v18 improves cost/latency but introduces refund regressions
- Foxhound detects, investigates, explains, validates, and governs the fix

### Demo system now supports
- overview
- executive summary
- traces
- trace detail
- run diff
- prompts
- prompt comparison
- regressions
- datasets
- experiments
- budgets
- SLAs
- notifications

This is now a solid base for a future marketing-site interactive demo.

---

## What still needs to be done

### Highest-priority next tasks

#### 1. Create a local review checklist artifact
Not yet done.

Recommended output:
- `docs/plans/testing/2026-04-13-demo-review-checklist.md`

Should include:
- exact routes to open
- exact seeded records to click
- exact narrative steps to verify
- expected observations for each page

This is the most useful immediate next artifact for real review.

#### 2. Browser QA / screenshot capture
Not yet done.

Recommended next action:
- run the demo locally
- verify each `/demo/*` page renders and links correctly
- capture screenshots for the strongest hero pages

Best target pages:
- `/demo`
- `/demo/executive`
- `/demo/traces`
- `/demo/traces/trace_support_refund_v18_regression`
- `/demo/diff?a=trace_support_refund_v17_baseline&b=trace_support_refund_v18_regression`
- `/demo/prompts/prompt_support_reply`
- `/demo/prompts/prompt_support_reply/diff?versionA=17&versionB=18`
- `/demo/regressions`
- `/demo/experiments`
- `/demo/budgets`
- `/demo/slas`
- `/demo/notifications`

#### 3. Add richer prompt-to-trace and diff-to-prompt linking
Partially done, but not fully polished everywhere.

Still worth checking:
- all demo prompt links open the most useful demo page
- prompt-focused querystring behavior is consistent in demo mode
- trace → prompt diff → experiment path is as smooth as possible

#### 4. Expand background trace generation
Background traces are present, but still relatively light.

Next improvement:
- add more low-noise healthy traffic
- add a bit more distribution by issue type / agent
- ensure `/demo/traces` filtering feels richer

#### 5. Decide whether to migrate current non-demo overview/executive placeholder data
The non-demo app still has hardcoded static placeholder story content in some pages.

Decision still pending:
- leave non-demo pages as-is for now, or
- unify fallback/demo behavior further using shared projections

#### 6. Prepare for future marketing-site consumption
Not yet wired.

Future work should:
- identify where the marketing interactive demo will live
- consume `packages/demo-domain` from that surface
- create a smaller `marketingLite` or `marketingHero` projection if needed

---

## Recommended next command-level work

If resuming this effort, the best next sequence is:

1. create a review checklist doc
2. run browser QA across `/demo/*`
3. capture screenshots / evidence
4. refine any broken or awkward navigation
5. only then consider additional scenario expansion or marketing-surface wiring

---

## Suggested resume prompt

Use something like:

> Continue the demo-domain work. Start by creating a local review checklist for the new `/demo` experience, then verify the key seeded pages render correctly and capture the strongest routes/screens for review.

---

## Summary

This session successfully transformed the demo from isolated `apps/web` fixture code into a reusable shared demo-domain architecture with a coherent narrative across most major product surfaces.

The main remaining work is no longer architecture. It is:
- reviewability
- evidence capture
- polish
- future marketing-surface consumption
